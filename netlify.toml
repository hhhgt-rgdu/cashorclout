[build]
  command = "npm run build && if [ -d restricted ]; then mkdir -p dist/restricted && cp -R restricted/. dist/restricted/; fi"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
